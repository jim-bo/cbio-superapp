"""cbio slash commands auto-discovered by cli-textual's CommandManager.

Each module in this package may define one or more ``SlashCommand`` subclasses
that are instantiated and registered when ``ChatApp`` is constructed with
``command_packages=["cbioportal.cli.slash_commands"]``.
"""
